---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id}>
  <h2 class="text-2xl font-bold mb-4 gradient-text">
    {title}
  </h2>
  <slot />
</section>
